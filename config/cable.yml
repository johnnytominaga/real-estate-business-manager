development:
  adapter: async

test:
  adapter: async

staging:
  adapter: redis
  url: <%= ENV['redis_cloud_url'] %>
  channel_prefix: real_estate_crm_staging

production:
  adapter: redis
  url: <%= ENV['redis_cloud_url'] %> #<%= ENV.fetch("REDIS_URL") { "redis://localhost:6379/1" } %>
  channel_prefix: real_estate_crm_production
