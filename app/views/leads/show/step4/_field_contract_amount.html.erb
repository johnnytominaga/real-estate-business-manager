<% if !lead_property.contract_amount.blank? %>
  <%= link_to edit_deposit_info_lead_path(lead_property_id: lead_property.id, property_id: lead_property.property_id, attribute: 'contract_amount'), remote: true do %><strong>Contract amount: </strong><%= number_to_currency(lead_property.contract_amount, unit: "€", precision: 0) %><% end %>
<% else %>
  <%= link_to edit_deposit_info_lead_path(lead_property_id: lead_property.id, property_id: lead_property.property_id, attribute: 'contract_amount'), remote: true do %><strong>Contract amount: </strong>Click to add<% end %>
<% end %>
