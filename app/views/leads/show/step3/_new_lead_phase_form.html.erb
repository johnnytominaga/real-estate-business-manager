<div class="row" style="margin: 10px 0;">
  <%= form_with model: @lead, url: create_lead_phase_lead_path(id: @lead), remote: true, method: :patch do |f| %>
    <div class="col-md-9">
      <%= f.fields_for :lead_phases, Phase.find(3).lead_phases.build do |s| %>
        <%= s.hidden_field :phase_id, value: 3 %>
        <%= s.text_field :comment, placeholder: "Enter Information Collection update...", class: 'new_lead_phase_field wow lightSpeedIn' %>
      <% end %>
    </div>
    <div class="col-md-3 wow fadeIn" data-wow-delay="0.1s" style="padding: 0px; display: inline-flex; justify-content: center;">
      <div class="actions">
        <%= button_tag(:name => "cancel", :class => "edit-button cancel cancel-icon wow zoomInDown", :id => "btnCancel", "data-wow-delay": "0.2s" ) do %><% end %>
      </div>
      <div class="actions">
        <%= button_tag(:name => "save", :class => "edit-button save save-icon wow zoomInDown", :id => "btnSubmit", "data-wow-delay": "0.3s") do %><% end %>
      </div>
    </div>
  <% end %>
</div>

<script>
  $('#btnSubmit, #btnCancel').click(function() {
    $("#spinner").fadeIn('slow');
    $("#spinner").click(function(){
      $("#spinner").hide();
    })
  });
</script>
