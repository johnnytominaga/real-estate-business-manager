<% unless @properties.empty? %>
  <%= will_paginate @properties, {link_options: {'data-remote': true}, params: { action: 'search_properties', q: @q }, renderer: BootstrapPagination::Rails, previous_label: "«", next_label: "»", inner_window: 1, outer_window: 0} %>
<% end %>
